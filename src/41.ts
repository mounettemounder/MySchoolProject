class ExampleClass:
    def __init__(self):
        self.some_attribute = "some_value"

    def some_method(self):
        print(f"Some method called with {self.some_attribute}.")

example_class = ExampleClass()
example_class.some_method()
